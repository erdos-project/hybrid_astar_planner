#ifndef CONSTANTS_H
#define CONSTANTS_H

// UI constants
const float START_POS_X = 50.0;
const float START_POS_Y = 50.0;
const float END_POS_X = 300.0;
const float END_POS_Y = 300.0;
const float NODE_RADIUS = 0.1;

// Algo constants
const float END_DIST_THRESHOLD = 2;
const float BOT_RADIUS = 1.0;
const float BOT_CLEARANCE = 1.5 * BOT_RADIUS;
const float LANE_WIDTH = 3.0;

#endif // CONSTANTS_H
